/* FUNCION ORIGINAL
 function example( a, b, c){
   return a + b + c;
 }
*/

// Transformando a una funcion de expresion// 
const example = function (a, b, c){
    return a + b + c;
};

//La funcion es asignada a una variable que es example//